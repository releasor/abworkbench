/**
 * TaskFlow ErrorBoundary — re-exports the shared ErrorBoundary from common.
 * This preserves backward compatibility for existing imports.
 */
export { default as ErrorBoundary } from '../../../components/common/ErrorBoundary';
